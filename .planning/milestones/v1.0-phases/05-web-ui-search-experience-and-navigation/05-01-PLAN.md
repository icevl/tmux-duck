---
phase: 05-web-ui-search-experience-and-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - web-ui/src/api.ts
  - web-ui/src/components/SessionSearch.tsx
  - web-ui/src/components/Sidebar.tsx
  - web-ui/src/styles.css
autonomous: true
requirements: [SRCH-01, SRCH-03, WEB-01, WEB-02, WEB-03, WEB-06, WEB-07]
user_setup: []
must_haves:
  truths:
    - "D-01 SRCH-01: Search entry appears in the existing sidebar without changing activeId or clearing the current ChatView draft."
    - "D-02 D-03 WEB-06 WEB-07: Active queries replace the session list with bounded backend results using limit and hits_per_session, not browser transcript scans."
    - "D-09 D-11 WEB-01 WEB-02: Search status and result states distinguish ready, indexing, degraded, unavailable, and no matches."
    - "D-10 D-12 SRCH-03: The search surface shows Open sessions only and exposes runtime, role/type, pinned, and recent filters."
    - "D-05 WEB-03: Selecting a result group opens its routing.window_id through the normal session selection path."
  artifacts:
    - path: "web-ui/src/api.ts"
      provides: "Search status/request/response DTOs and API helpers"
      contains: "searchSessions"
    - path: "web-ui/src/components/SessionSearch.tsx"
      provides: "Sidebar search input, filters, status chip, grouped results, and nested snippets"
      min_lines: 120
    - path: "web-ui/src/components/Sidebar.tsx"
      provides: "Search component mounted above session list with normal list preserved for empty query"
      contains: "SessionSearch"
    - path: "web-ui/src/styles.css"
      provides: "Responsive sidebar search and result styling"
      contains: "session-search"
  key_links:
    - from: "web-ui/src/components/SessionSearch.tsx"
      to: "/api/search/status"
      via: "api.getSearchStatus"
      pattern: "getSearchStatus"
    - from: "web-ui/src/components/SessionSearch.tsx"
      to: "/api/search"
      via: "api.searchSessions"
      pattern: "searchSessions"
    - from: "web-ui/src/components/Sidebar.tsx"
      to: "App.tsx session selection"
      via: "onOpenResult or onSelect callback carrying routing.window_id"
      pattern: "onOpen"
---

<objective>
Build the browser search entry point and bounded result browsing surface in the existing sidebar.

Purpose: Users need to search active sessions from the normal Web UI workflow while keeping the current session and draft input untouched.
Output: TypeScript search DTOs/API helpers, a new sidebar-owned search component, Sidebar integration, and CSS for compact status/results/filter states.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-CONTEXT.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-RESEARCH.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-UI-SPEC.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-PATTERNS.md
@src/codexbot/search/contracts.py
@web-ui/src/api.ts
@web-ui/src/components/Sidebar.tsx
@web-ui/src/styles.css
</context>

<threat_model>
ASVS level: 1
Block on: high

| Threat | Severity | Component | Mitigation |
| --- | --- | --- | --- |
| T-05-01 Browser loads or indexes full transcripts while searching long histories. | high | `SessionSearch.tsx` | Search UI must call `api.searchSessions` with bounded `limit` and `hits_per_session`; it must never call `api.getMessages` or scan `SessionMessage.text` for global search. |
| T-05-02 Search API auth or cookie handling diverges from the existing Web UI wrapper. | high | `api.ts` | Add helpers through existing `request<T>()`, preserving `credentials: "include"` and 401 handling. |
| T-05-03 Unavailable or degraded search is displayed as ordinary no matches. | medium | `SessionSearch.tsx` | Render distinct state panels from `status.state`, `status.available`, and `SearchResponse.outcome`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add Web UI search DTOs and API helpers</name>
  <files>web-ui/src/api.ts</files>
  <read_first>web-ui/src/api.ts, src/codexbot/search/contracts.py</read_first>
  <action>Add TypeScript interfaces mirroring the backend snake_case JSON contract: `SearchIndexState`, `SearchResponseOutcome`, `SearchOutcome`, `SearchCounters`, `SearchGenerationMetadata`, `SearchIndexMetadata`, `SearchStatusResponse`, `SearchRoutingMetadata`, `SearchRowIdentity`, `TranscriptProvenance`, `SearchHighlight`, `SearchHit`, `SearchSessionResult`, `SearchRequest`, and `SearchResponse`. Add `api.getSearchStatus()` calling `GET /api/search/status` and `api.searchSessions(request)` calling `POST /api/search`. Extend `api.getMessages` options only if needed by TypeScript imports in this plan; exact `around_*` support belongs to Plan 02.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/api.ts` exports `SearchStatusResponse`.
    - `web-ui/src/api.ts` exports `SearchResponse`.
    - `web-ui/src/api.ts` contains `getSearchStatus: () =>`.
    - `web-ui/src/api.ts` contains `searchSessions:`.
    - `api.searchSessions` uses `method: "POST"` and `json: request`.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Search API types and helpers compile and preserve the existing request wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Create the sidebar search component</name>
  <files>web-ui/src/components/SessionSearch.tsx</files>
  <read_first>web-ui/src/components/Sidebar.tsx, web-ui/src/api.ts, .planning/phases/05-web-ui-search-experience-and-navigation/05-UI-SPEC.md, .planning/phases/05-web-ui-search-experience-and-navigation/05-PATTERNS.md</read_first>
  <action>Create `SessionSearch.tsx` with props for `sessions`, `onOpenResult(windowId)`, `onOpenHit(target)`, and `onHasActiveQueryChange(active)`. Own local query, debounced query, status, loading, error, result, runtime filter, role/type filter, pinned filter, and recent filter state. Use a meaningful query threshold of at least 2 trimmed characters. Send `limit: 10` and `hits_per_session: 3` by default. Render the exact UI-SPEC strings `Search open sessions`, `Open sessions only`, `Searching...`, `No matches`, `Try different terms or filters.`, `Indexing`, `Results may be incomplete.`, `Degraded`, `Search unavailable`, and `Keep working and try again after indexing recovers.` Render runtime filters `All`, `Codex`, `Claude`; role/type filters `All`, `User`, `Agent`, `Tool`; pinned toggle; recent filters `Any`, `1h`, `24h`, `7d`. Use lucide icons such as `Search`, `X`, `Filter`, `AlertTriangle`, and `Loader2`. Do not import `api.getMessages` and do not inspect transcript text from `sessions` or chat messages.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/components/SessionSearch.tsx` contains `Search open sessions`.
    - `web-ui/src/components/SessionSearch.tsx` contains `Open sessions only`.
    - `web-ui/src/components/SessionSearch.tsx` passes `limit: 10`.
    - `web-ui/src/components/SessionSearch.tsx` passes `hits_per_session: 3`.
    - `web-ui/src/components/SessionSearch.tsx` calls `api.getSearchStatus`.
    - `web-ui/src/components/SessionSearch.tsx` calls `api.searchSessions`.
    - `web-ui/src/components/SessionSearch.tsx` does not call `api.getMessages`.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Users can type a bounded debounced query, see status/scope/filter controls, and inspect grouped results with nested snippets.</done>
</task>

<task type="auto">
  <name>Task 3: Mount search in Sidebar without breaking session ordering</name>
  <files>web-ui/src/components/Sidebar.tsx</files>
  <read_first>web-ui/src/components/Sidebar.tsx, web-ui/src/components/SessionSearch.tsx, .planning/phases/05-web-ui-search-experience-and-navigation/05-CONTEXT.md</read_first>
  <action>Import and render `SessionSearch` below the `New session` action and above the normal `.session-list` body. Add Sidebar props for hit navigation only if needed by Plan 01: `onOpenSearchHit`. Keep the existing `ordered` derivation and drag/drop session list code unchanged for empty query. Track whether search has an active query via `onHasActiveQueryChange`; when active, hide the normal session rows and let `SessionSearch` render result rows. Group-result clicks must call the same selection path as normal session rows with `routing.window_id`. Do not mutate `sessions`, `sort_order`, or pinned state from search rendering.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/components/Sidebar.tsx` imports `SessionSearch`.
    - `web-ui/src/components/Sidebar.tsx` renders `<SessionSearch`.
    - Empty-query rendering still contains the existing `ordered.map((s) =>` session-list branch.
    - Existing `moveSession` and `onReorder` logic remains present.
    - Group click path calls `onSelect` or an equivalent callback with the result `window_id`.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Search appears in the sidebar and preserves the normal session list behavior when inactive.</done>
</task>

<task type="auto">
  <name>Task 4: Add compact search styling</name>
  <files>web-ui/src/styles.css</files>
  <read_first>web-ui/src/styles.css, .planning/phases/05-web-ui-search-experience-and-navigation/05-UI-SPEC.md</read_first>
  <action>Add CSS classes prefixed with `session-search`, `search-status`, `search-scope`, `search-filter`, and `search-result`. Use existing tokens `--bg-1`, `--bg-2`, `--bg-elevated`, `--border`, `--border-strong`, `--text-0`, `--text-1`, `--text-2`, `--accent`, `--ok`, `--warn`, and `--danger`. Keep rows compact with 6px radius and 8px to 12px spacing. Clamp snippets to 2 lines on mobile and 3 lines on desktop. Ensure result text wraps or ellipsizes and cannot create horizontal body scroll. Avoid nested page-section cards, gradients, and new palette tokens.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/styles.css` contains `.session-search`.
    - `web-ui/src/styles.css` contains `.search-result-hit`.
    - `web-ui/src/styles.css` contains `Open sessions only` nowhere; copy stays in JSX, not CSS.
    - `web-ui/src/styles.css` uses `--accent` for active/focused search affordances.
    - `web-ui/src/styles.css` includes mobile rules for search snippets or search result rows under the existing `max-width: 760px` media section.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Search UI fits the existing dark operational sidebar on desktop and mobile.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir web-ui build` exits 0.
- [ ] `rg -n "api\\.getMessages" web-ui/src/components/SessionSearch.tsx` returns no matches.
- [ ] Search query/state is local to the sidebar and does not clear `ChatView` composer drafts.
- [ ] Empty query still renders the normal ordered session list and drag/drop controls.
</verification>

<success_criteria>

- Users can start search from the sidebar without switching sessions.
- Search status, scope, filters, no-match, indexing, degraded, and unavailable states are visible.
- Search requests are debounced and bounded by backend request fields.
- Group results open sessions by `routing.window_id`.
- No browser-side full transcript search is introduced.

</success_criteria>

<output>
After completion, create `.planning/phases/05-web-ui-search-experience-and-navigation/05-01-SUMMARY.md`.
</output>
