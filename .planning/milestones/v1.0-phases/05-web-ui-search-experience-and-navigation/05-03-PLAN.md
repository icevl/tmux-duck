---
phase: 05-web-ui-search-experience-and-navigation
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - web-ui/src/components/SessionSearch.tsx
  - web-ui/src/components/Sidebar.tsx
  - web-ui/src/App.tsx
  - web-ui/src/components/ChatView.tsx
  - web-ui/src/styles.css
  - tests/codexbot/test_web_api.py
autonomous: true
requirements: [SRCH-01, SRCH-03, WEB-01, WEB-02, WEB-03, WEB-04, WEB-05, WEB-06, WEB-07]
user_setup: []
must_haves:
  truths:
    - "D-01 SRCH-01: Search interactions do not clear draft input or interrupt sending messages in the active session."
    - "D-04: On mobile, selecting a group or hit closes the sidebar drawer while preserving the query/results for later inspection."
    - "D-09 D-10 D-11 D-12: Status, filter, scope, and result states remain readable inside width min(86vw, 320px)."
    - "WEB-07: Search remains responsive through debouncing and capped result payloads with no browser full-history indexing."
    - "All Phase 5 validation commands pass or any unavailable manual smoke step is recorded with a concrete reason in the SUMMARY."
  artifacts:
    - path: "web-ui/src/components/SessionSearch.tsx"
      provides: "Mobile-safe result rendering and final state handling"
      contains: "Open sessions only"
    - path: "web-ui/src/styles.css"
      provides: "Responsive search, filter, snippet, and highlight rules"
      contains: "@media (max-width: 760px)"
    - path: "tests/codexbot/test_web_api.py"
      provides: "Backend route regression tests for search and message windows"
      contains: "test_search"
  key_links:
    - from: "web-ui/src/components/SessionSearch.tsx"
      to: "web-ui/src/components/Sidebar.tsx"
      via: "active query replaces session-list body"
      pattern: "onHasActiveQueryChange"
    - from: "web-ui/src/components/ChatView.tsx"
      to: "web-ui/src/styles.css"
      via: "search-hit row class and label"
      pattern: "search-hit"
---

<objective>
Harden the search experience across mobile, state-copy, prompt placement, and final validation.

Purpose: Users should be able to use search from the browser workflow on desktop and mobile without losing context, confusing index states, or regressing existing chat/session controls.
Output: Responsive polish, regression fixes, validation updates, and final checks for all Phase 5 Web UI search behaviors.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-CONTEXT.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-RESEARCH.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-UI-SPEC.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-VALIDATION.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-PATTERNS.md
@web-ui/src/components/SessionSearch.tsx
@web-ui/src/components/Sidebar.tsx
@web-ui/src/components/ChatView.tsx
@web-ui/src/styles.css
@tests/codexbot/test_web_api.py
</context>

<threat_model>
ASVS level: 1
Block on: high

| Threat | Severity | Component | Mitigation |
| --- | --- | --- | --- |
| T-05-07 Mobile search controls overflow the drawer or obscure the chat after selection. | medium | `styles.css`, `Sidebar.tsx` | Use the existing `max-width: 760px` drawer rules, wrapping filters, clamped snippets, and close the drawer after group/hit selection. |
| T-05-08 Search feature regresses existing slash hints, skill hints, session ordering, chat send, or notifications. | high | `SessionSearch.tsx`, `Sidebar.tsx`, `ChatView.tsx` | Keep search state isolated, preserve empty-query session list code, and run frontend build plus targeted backend tests. |
| T-05-09 Manual visual behaviors silently fail because no browser smoke was attempted. | medium | Web UI manual validation | Attempt a local smoke if a dev server is available; otherwise record the blocker in the plan summary with the exact command not run. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify mobile drawer behavior and result layout</name>
  <files>web-ui/src/components/SessionSearch.tsx, web-ui/src/components/Sidebar.tsx, web-ui/src/styles.css</files>
  <read_first>web-ui/src/components/SessionSearch.tsx, web-ui/src/components/Sidebar.tsx, web-ui/src/styles.css, .planning/phases/05-web-ui-search-experience-and-navigation/05-UI-SPEC.md</read_first>
  <action>Ensure search controls fit within the existing mobile sidebar width `min(86vw, 320px)`. Filters must wrap instead of shrinking text below readable sizes. Snippets must clamp to 2 lines on mobile and 3 lines on desktop. Group and hit selection must close the mobile drawer through existing `onClose` or session-selection behavior while preserving the search component's query/results state for when the drawer is reopened. Confirm normal empty-query session list drag/drop still works and pinned sessions remain visually identifiable.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/styles.css` contains mobile rules for `.session-search`.
    - `web-ui/src/styles.css` contains `-webkit-line-clamp: 2` for mobile search snippets or equivalent mobile-only snippet clamping.
    - `web-ui/src/components/SessionSearch.tsx` keeps query state local and does not reset it from `onOpenResult`.
    - `web-ui/src/components/Sidebar.tsx` still renders pinned session indicators in the normal list.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Search is usable inside the mobile drawer and normal session list behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm input prompts and composer drafts survive search navigation</name>
  <files>web-ui/src/App.tsx, web-ui/src/components/ChatView.tsx, web-ui/src/components/SessionSearch.tsx</files>
  <read_first>web-ui/src/App.tsx, web-ui/src/components/ChatView.tsx, web-ui/src/components/SessionSearch.tsx, .planning/phases/05-web-ui-search-experience-and-navigation/05-CONTEXT.md</read_first>
  <action>Review and adjust the final search target flow so `handleOpenSearchHit` changes only active session and search target state. It must not clear `draftsRef`, `textRef`, `choicePageByKey`, or `choiceSendingKey` beyond the existing session-switch effects. `ChatView` must keep active input-required choice prompts in the existing bottom-rendered branch. If search navigation scrolls to an older row while an active prompt exists, the prompt remains active and answer buttons remain enabled until the user chooses an option.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/App.tsx` does not clear composer draft state in `handleOpenSearchHit`.
    - `web-ui/src/components/ChatView.tsx` still filters `activeChoiceMessage` out of `displayMessages`.
    - `web-ui/src/components/ChatView.tsx` still renders `activeChoiceMessage` after `streaming`.
    - `web-ui/src/components/SessionSearch.tsx` does not import or mutate choice prompt state.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Search navigation does not disrupt draft text or active user-input prompt choices.</done>
</task>

<task type="auto">
  <name>Task 3: Run Phase 5 targeted and full validation</name>
  <files>tests/codexbot/test_web_api.py, web-ui/src/api.ts, web-ui/src/components/SessionSearch.tsx, web-ui/src/components/ChatView.tsx, web-ui/src/styles.css</files>
  <read_first>.planning/phases/05-web-ui-search-experience-and-navigation/05-VALIDATION.md, AGENTS.md, tests/codexbot/test_web_api.py</read_first>
  <action>Run Phase 5 validation commands. First run targeted checks `uv run pytest tests/codexbot/test_web_api.py -q` and `pnpm --dir web-ui build`. Then run the full repository lane from AGENTS and VALIDATION: `uv run ruff check src/ tests/`, `uv run pyright src/codexbot/`, `/tmp/codexbot-venv/bin/pytest -q`, and `pnpm --dir web-ui build`. If a local Web UI dev server is already running or can be started without disrupting the user's service, perform a brief mobile-width smoke of search input, status display, group open, hit open, and drawer close; otherwise record in SUMMARY that manual smoke was not run and why.</action>
  <verify>uv run ruff check src/ tests/ && uv run pyright src/codexbot/ && /tmp/codexbot-venv/bin/pytest -q && pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `uv run pytest tests/codexbot/test_web_api.py -q` exits 0.
    - `pnpm --dir web-ui build` exits 0.
    - `uv run ruff check src/ tests/` exits 0.
    - `uv run pyright src/codexbot/` exits 0.
    - `/tmp/codexbot-venv/bin/pytest -q` exits 0.
    - The plan summary records whether mobile/manual smoke was run; if not, it records the concrete reason.
  </acceptance_criteria>
  <done>All automated Phase 5 validation commands pass and manual smoke status is documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/codexbot/test_web_api.py -q` exits 0.
- [ ] `pnpm --dir web-ui build` exits 0.
- [ ] `uv run ruff check src/ tests/` exits 0.
- [ ] `uv run pyright src/codexbot/` exits 0.
- [ ] `/tmp/codexbot-venv/bin/pytest -q` exits 0.
- [ ] Manual mobile smoke is run or the exact blocker is recorded in the SUMMARY.
</verification>

<success_criteria>

- Mobile users can search, inspect results, open sessions, and return to chat without horizontal scrolling.
- Existing session ordering, pinned sessions, slash hints, skill hints, notifications, and chat input continue to work.
- Active user-input prompts remain active and bottom-positioned until answered.
- All Phase 5 requirements are covered by passing automated checks and recorded manual-smoke status.

</success_criteria>

<output>
After completion, create `.planning/phases/05-web-ui-search-experience-and-navigation/05-03-SUMMARY.md`.
</output>
