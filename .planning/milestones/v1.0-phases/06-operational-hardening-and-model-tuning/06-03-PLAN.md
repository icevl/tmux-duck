---
phase: 06-operational-hardening-and-model-tuning
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - pyproject.toml
  - README.md
  - src/codexbot/search/benchmark.py
  - src/codexbot/search/contracts.py
  - src/codexbot/search/state.py
  - src/codexbot/search/client.py
  - src/codexbot/search/worker.py
  - web-ui/src/api.ts
  - tests/codexbot/test_search_benchmark.py
  - tests/codexbot/test_search_contracts.py
  - tests/codexbot/test_search_worker.py
  - tests/codexbot/test_web_api.py
  - tests/fixtures/search/benchmark_cases.json
autonomous: true
requirements:
  - OPS-05
  - OPS-06
requirements_addressed: [OPS-05, OPS-06]
user_setup:
  - "Optional: run the real Qwen benchmark on the target Mac-mini-class host after implementation."
must_haves:
  truths:
    - "D-09 OPS-05: Local benchmark uses representative Codi transcript fixtures and covers embedding, LanceDB upsert/index creation, query latency, and degraded fallback behavior."
    - "D-10 OPS-05: Thresholds and baselines are configurable and recorded; the implementation must not hard-code one machine profile."
    - "D-11 OPS-06: `Qwen/Qwen3-Embedding-0.6B` remains the default pending validation; if validation fails or model files are unavailable, lexical-only degraded search remains available and documented."
    - "D-12 OPS-05: Benchmark execution is opt-in and normal unit/type/frontend checks do not require loading large model/index dependencies."
    - "D-04 OPS-06: Web UI recovery remains read-only; benchmark and rebuild commands are displayed or documented, not executed by browser controls."
  artifacts:
    - path: "src/codexbot/search/benchmark.py"
      provides: "Opt-in local benchmark CLI with structured JSON metrics"
      contains: "benchmark"
    - path: "tests/codexbot/test_search_benchmark.py"
      provides: "Fake-provider benchmark schema and fallback tests"
      contains: "benchmark"
    - path: "README.md"
      provides: "Documented search degraded/fallback and benchmark commands"
      contains: "codexbot-search-benchmark"
  key_links:
    - from: "src/codexbot/search/benchmark.py"
      to: "src/codexbot/search/index.py"
      via: "fixture documents are embedded, indexed, and queried locally"
      pattern: "materialize|query|metrics"
    - from: "src/codexbot/search/benchmark.py"
      to: "src/codexbot/search/state.py"
      via: "latest benchmark summary is persisted under search-owned state"
      pattern: "benchmark"
---

<objective>
Add the opt-in local benchmark and model/default validation path for Phase 6,
then document Qwen readiness and lexical fallback behavior.

Purpose: Codi needs a repeatable way to validate Mac-mini-appropriate embedding
throughput, memory, batch size, chunk size, query latency, and degraded behavior
without making normal tests load Qwen.
Output: benchmark CLI/module, sanitized fixture set, benchmark summary state,
status exposure, tests with fake providers, README documentation, and optional
real Qwen smoke instructions.
</objective>

<execution_context>
@/home/vlupanov/.codex/get-shit-done/workflows/execute-plan.md
@/home/vlupanov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-CONTEXT.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-RESEARCH.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-VALIDATION.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-AI-SPEC.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-PATTERNS.md
@src/codexbot/search/embedding.py
@src/codexbot/search/index.py
@src/codexbot/search/retrieval.py
@src/codexbot/search/state.py
@src/codexbot/search/worker.py
@src/codexbot/search/client.py
@README.md
@pyproject.toml
</context>

<threat_model>
ASVS level: 1
Block on: high

| Threat | Severity | Component | Mitigation |
| --- | --- | --- | --- |
| T-06-09 Benchmark loads Qwen during normal tests or service startup. | high | `benchmark.py`, `worker.py`, tests | Benchmark is opt-in; normal tests use fake provider and do not import/load real model packages beyond existing lazy boundaries. |
| T-06-10 Benchmark output leaks transcript content, secrets, or local paths. | high | `benchmark.py`, status summary | Fixture inputs are sanitized; persisted summary stores metrics and model metadata only, not raw documents. |
| T-06-11 One local machine baseline is hard-coded as universal. | medium | `benchmark.py`, README | Thresholds come from CLI/config/env defaults and are reported in JSON; docs state target-host validation is required. |
| T-06-12 Failed Qwen validation blocks all search. | high | `client.py`, docs | Preserve lexical-only degraded behavior and document `CODEXBOT_SEARCH_MODEL_ID`, `CODEXBOT_SEARCH_VECTOR_DIM`, and `CODEXBOT_SEARCH_LOCAL_FILES_ONLY`. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock benchmark schema, fake-provider behavior, and fixture shape in tests</name>
  <files>tests/codexbot/test_search_benchmark.py, tests/fixtures/search/benchmark_cases.json</files>
  <read_first>
    .planning/phases/06-operational-hardening-and-model-tuning/06-AI-SPEC.md
    .planning/phases/06-operational-hardening-and-model-tuning/06-RESEARCH.md
    src/codexbot/search/embedding.py
    src/codexbot/search/index.py
    src/codexbot/search/retrieval.py
    tests/codexbot/test_search_index.py
    tests/codexbot/test_search_retrieval.py
  </read_first>
  <action>Create `tests/fixtures/search/benchmark_cases.json` with sanitized transcript-like documents and query cases covering exact path/command/error lookup, semantic paraphrase lookup, metadata/runtime cases, and degraded fallback. Add `tests/codexbot/test_search_benchmark.py` with fake embedding provider tests that assert benchmark output contains `ok`, `model_id`, `vector_dimension`, `batch_size`, `chunk_max_chars`, `chunk_overlap_chars`, `document_count`, `query_count`, `embedding_docs_per_second`, `peak_memory_mb`, `index_elapsed_ms`, `query_p50_ms`, `query_p95_ms`, `exact_top3`, `semantic_top5`, `fallback_ok`, `package_versions`, and `thresholds`. Assert the fake-provider benchmark does not require network or real Qwen weights. Assert raw fixture text is not persisted in the benchmark summary.</action>
  <verify>uv run pytest tests/codexbot/test_search_benchmark.py -q</verify>
  <acceptance_criteria>
    - `tests/fixtures/search/benchmark_cases.json` exists and contains at least 8 documents and 6 queries.
    - `tests/codexbot/test_search_benchmark.py` asserts the benchmark JSON schema.
    - Tests use fake provider injection and pass without Qwen download.
    - Tests assert persisted summary excludes raw document text.
  </acceptance_criteria>
  <done>Benchmark behavior is test-locked before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement opt-in benchmark CLI and summary state</name>
  <files>pyproject.toml, src/codexbot/search/benchmark.py, src/codexbot/search/contracts.py, src/codexbot/search/state.py, src/codexbot/search/client.py, src/codexbot/search/worker.py, tests/codexbot/test_search_benchmark.py, tests/codexbot/test_search_worker.py, tests/codexbot/test_search_contracts.py, tests/codexbot/test_web_api.py</files>
  <read_first>
    tests/codexbot/test_search_benchmark.py
    tests/codexbot/test_search_worker.py
    tests/codexbot/test_search_contracts.py
    tests/codexbot/test_web_api.py
    pyproject.toml
    src/codexbot/search/embedding.py
    src/codexbot/search/index.py
    src/codexbot/search/retrieval.py
    src/codexbot/search/state.py
    src/codexbot/search/client.py
    src/codexbot/search/worker.py
  </read_first>
  <action>Add `src/codexbot/search/benchmark.py` with `main(argv: Sequence[str] | None = None) -> int` and a reusable `run_benchmark(...)` function. Support CLI flags `--fixtures`, `--model`, `--provider fake|local`, `--batch-size`, `--chunk-max-chars`, `--chunk-overlap-chars`, `--threshold-query-p95-ms`, `--threshold-memory-mb`, and `--write-summary`. Use stdlib timing and `tracemalloc` for memory so no new runtime dependency is required. For fake provider mode, use deterministic vectors and no model imports. For local provider mode, use the existing `SentenceTransformerEmbeddingProvider`. Add a `codexbot-search-benchmark = codexbot.search.benchmark:main` script in `pyproject.toml` and optionally a `benchmark` subcommand in `codexbot-search-worker` that delegates to the same module. Add search-owned state helpers `benchmark_summary_path()`, `read_benchmark_summary()`, and `write_benchmark_summary()` under `CODEXBOT_DIR/search`. Persist only metrics, model/config/package metadata, thresholds, and pass/fail status. Extend `SearchBenchmarkSummary` and `SearchStatusResponse.operations.benchmark` so `/api/search/status` includes the latest summary when present.</action>
  <verify>uv run pytest tests/codexbot/test_search_benchmark.py tests/codexbot/test_search_worker.py tests/codexbot/test_search_contracts.py tests/codexbot/test_web_api.py -q</verify>
  <acceptance_criteria>
    - Source assertion: `pyproject.toml` contains `codexbot-search-benchmark`.
    - CLI assertion: `uv run python -m codexbot.search.benchmark --fixtures tests/fixtures/search/benchmark_cases.json --provider fake --write-summary` exits 0.
    - Behavior assertion: summary state is written under `CODEXBOT_DIR/search` and read by status.
    - Behavior assertion: benchmark JSON includes model/config/package metadata and pass/fail thresholds.
    - Security assertion: persisted summary contains no raw transcript/document text.
    - Normal targeted tests pass without loading real Qwen.
  </acceptance_criteria>
  <done>Codi has an opt-in benchmark path and status-visible benchmark summary.</done>
</task>

<task type="auto">
  <name>Task 3: Document model defaults, fallback behavior, and validation commands</name>
  <files>README.md, web-ui/src/api.ts, tests/codexbot/test_search_contracts.py, tests/codexbot/test_web_api.py</files>
  <read_first>
    README.md
    .planning/phases/06-operational-hardening-and-model-tuning/06-AI-SPEC.md
    .planning/phases/06-operational-hardening-and-model-tuning/06-CONTEXT.md
    web-ui/src/api.ts
    tests/codexbot/test_search_contracts.py
    tests/codexbot/test_web_api.py
  </read_first>
  <action>Add a concise `Search operations` section to `README.md`. Document default model `Qwen/Qwen3-Embedding-0.6B`, vector dimension `1024`, default batch size `16`, environment overrides `CODEXBOT_SEARCH_MODEL_ID`, `CODEXBOT_SEARCH_VECTOR_DIM`, `CODEXBOT_SEARCH_BATCH_SIZE`, `CODEXBOT_SEARCH_LOCAL_FILES_ONLY`, and `CODEXBOT_SEARCH_WORKER_STALE_SECONDS`. Document degraded modes: no generation means no results while indexing/backfill status is shown; completed generation without semantic index means lexical degraded search; semantic/model/LanceDB failure means lexical degraded search when generation documents exist; failed/stale worker status does not block chat/session/terminal/Telegram/WebSocket paths. Document commands `codexbot-search-worker smoke-search-index`, `codexbot-search-worker live-drain-once`, `codexbot-search-worker rebuild`, and `codexbot-search-benchmark --fixtures tests/fixtures/search/benchmark_cases.json --provider fake`. Add tests or source assertions for these README strings if there is an existing docs-contract pattern; otherwise verify with `rg` commands in the summary.</action>
  <verify>uv run pytest tests/codexbot/test_search_contracts.py tests/codexbot/test_web_api.py -q && rg -n "Search operations|Qwen/Qwen3-Embedding-0.6B|codexbot-search-benchmark|CODEXBOT_SEARCH_MODEL_ID|lexical degraded" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `Search operations`.
    - `README.md` contains `Qwen/Qwen3-Embedding-0.6B`.
    - `README.md` contains `codexbot-search-benchmark`.
    - `README.md` contains `CODEXBOT_SEARCH_MODEL_ID`, `CODEXBOT_SEARCH_VECTOR_DIM`, `CODEXBOT_SEARCH_BATCH_SIZE`, and `CODEXBOT_SEARCH_LOCAL_FILES_ONLY`.
    - `README.md` describes lexical degraded fallback and unavailable/no-generation behavior.
  </acceptance_criteria>
  <done>OPS-06 fallback/degraded behavior and OPS-05 validation commands are documented.</done>
</task>

<task type="auto">
  <name>Task 4: Run final local validation and record optional real-model status</name>
  <files>.planning/phases/06-operational-hardening-and-model-tuning/06-03-SUMMARY.md</files>
  <read_first>
    .planning/phases/06-operational-hardening-and-model-tuning/06-VALIDATION.md
    README.md
    tests/codexbot/test_search_benchmark.py
    AGENTS.md
  </read_first>
  <action>Run the final validation lane from `AGENTS.md` with the repository-supported full-suite command. Run the fake-provider benchmark command and include the JSON metrics summary in `06-03-SUMMARY.md`. If the real Qwen benchmark is practical on the current host, run `uv run python -m codexbot.search.benchmark --fixtures tests/fixtures/search/benchmark_cases.json --model Qwen/Qwen3-Embedding-0.6B`; otherwise record that real-model validation is a target-host manual verification and do not block normal completion. Do not treat first-time model download absence as a code failure if lexical degraded behavior is passing and documented.</action>
  <verify>uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run pyright src/codexbot/ && uv run pytest -q && pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - Full validation command exits 0 or the exact environment blocker is recorded in `06-03-SUMMARY.md`.
    - Fake-provider benchmark exits 0 and writes structured summary state.
    - Real Qwen benchmark outcome is recorded if run; if skipped, summary explains it is target-host optional/manual.
    - `06-03-SUMMARY.md` lists Qwen default decision or fallback status based on available evidence.
  </acceptance_criteria>
  <done>Phase 6 has validation evidence and a recorded model/default decision path.</done>
</task>

</tasks>

<verification>
Run after this plan:

<automated>uv run pytest tests/codexbot/test_search_benchmark.py tests/codexbot/test_search_worker.py tests/codexbot/test_search_contracts.py tests/codexbot/test_web_api.py -q</automated>
<automated>uv run ruff check src/ tests/ && uv run ruff format --check src/ tests/ && uv run pyright src/codexbot/ && uv run pytest -q && pnpm --dir web-ui build</automated>
<manual>Optional target-host Qwen benchmark: `uv run python -m codexbot.search.benchmark --fixtures tests/fixtures/search/benchmark_cases.json --model Qwen/Qwen3-Embedding-0.6B`</manual>
</verification>

<success_criteria>
- OPS-05 is covered by an opt-in local benchmark path for embedding throughput, memory, batch size, chunk size, and query latency.
- OPS-06 is covered by documented lexical degraded and unavailable behavior when semantic embedding is not ready.
- D-09 through D-12 are implemented and documented.
- Normal tests remain dependency-light and do not require loading the real Qwen model.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operational-hardening-and-model-tuning/06-03-SUMMARY.md`.
</output>
