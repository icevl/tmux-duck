---
phase: 06-operational-hardening-and-model-tuning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codexbot/search/contracts.py
  - src/codexbot/search/client.py
  - src/codexbot/search/state.py
  - src/codexbot/web/api.py
  - web-ui/src/api.ts
  - web-ui/src/components/SessionSearch.tsx
  - web-ui/src/styles.css
  - tests/codexbot/test_search_contracts.py
  - tests/codexbot/test_search_worker.py
  - tests/codexbot/test_web_api.py
  - tests/codexbot/test_web_ui_search_contract.py
autonomous: true
requirements:
  - OPS-03
  - OPS-06
requirements_addressed: [OPS-03, OPS-06]
user_setup: []
must_haves:
  truths:
    - "D-01 OPS-03: Worker heartbeat, queue lag, indexed/open counts, backfill progress, and recent indexing errors appear through the existing search panel and authenticated status route."
    - "D-02 OPS-03: Default UI remains compact; detailed heartbeat, queue lag, progress, and errors are hidden behind `Show details` / `Hide details`."
    - "D-03 OPS-03 OPS-06: Status refresh uses modest polling of `/api/search/status` and search-response refreshes without embedding or indexing work in chat/session/terminal/WebSocket paths."
    - "D-04 OPS-06: Browser recovery guidance is read-only command text, not retry/rebuild mutation controls."
    - "D-09 OPS-05: Benchmark/model metadata fields may be nullable in this plan but must have a typed place in status for Plan 06-03."
  artifacts:
    - path: "src/codexbot/search/contracts.py"
      provides: "Typed operational search status DTOs"
      contains: "SearchOperationalStatus"
    - path: "src/codexbot/search/client.py"
      provides: "Request-safe operational status derivation"
      contains: "get_status"
    - path: "web-ui/src/components/SessionSearch.tsx"
      provides: "Expandable search status details inside the existing sidebar search panel"
      contains: "Show details"
    - path: "web-ui/src/styles.css"
      provides: "Compact desktop/mobile search status details styling"
      contains: "search-status-details"
  key_links:
    - from: "src/codexbot/web/api.py"
      to: "src/codexbot/search/client.py"
      via: "`/api/search/status` calls `search_client.get_status()`"
      pattern: "get_status"
    - from: "web-ui/src/components/SessionSearch.tsx"
      to: "/api/search/status"
      via: "api.getSearchStatus"
      pattern: "getSearchStatus"
---

<objective>
Add the Phase 6 operational search status contract and render it as compact,
expandable details inside the existing Web UI search panel.

Purpose: users need to see worker heartbeat, queue lag, indexed/open counts,
backfill progress, recent indexing errors, and read-only recovery guidance
without opening a separate admin panel or blocking normal Codi workflows.
Output: typed backend status details, authenticated status API payload,
frontend DTOs, accessible details UI, CSS, and static/backend tests.
</objective>

<execution_context>
@/home/vlupanov/.codex/get-shit-done/workflows/execute-plan.md
@/home/vlupanov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-CONTEXT.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-RESEARCH.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-VALIDATION.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-UI-SPEC.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-AI-SPEC.md
@.planning/phases/06-operational-hardening-and-model-tuning/06-PATTERNS.md
@src/codexbot/search/contracts.py
@src/codexbot/search/client.py
@src/codexbot/search/queue.py
@src/codexbot/search/state.py
@src/codexbot/web/api.py
@web-ui/src/api.ts
@web-ui/src/components/SessionSearch.tsx
@web-ui/src/styles.css
@tests/codexbot/test_search_contracts.py
@tests/codexbot/test_search_worker.py
@tests/codexbot/test_web_api.py
@tests/codexbot/test_web_ui_search_contract.py
</context>

<threat_model>
ASVS level: 1
Block on: high

| Threat | Severity | Component | Mitigation |
| --- | --- | --- | --- |
| T-06-01 Raw worker or queue errors leak secrets, local paths, stack traces, or transcript text through status details. | high | `contracts.py`, `client.py`, `queue.py` | Status DTOs must expose bounded sanitized strings only; tests assert forbidden strings are absent. |
| T-06-02 Request status route imports or loads model/index dependencies. | high | `web/api.py`, `search/client.py` | Keep `get_status()` dependency-light and extend existing import-boundary tests for `lancedb`, `torch`, `transformers`, and `sentence_transformers`. |
| T-06-03 Status details overload the sidebar or cover mobile chat controls. | medium | `SessionSearch.tsx`, `styles.css` | Details stay inside the existing sidebar search area, wrap long text, and are hidden by default. |
| T-06-04 Browser recovery controls mutate local search state accidentally. | medium | `SessionSearch.tsx` | Recovery commands render as read-only text only; no rebuild/retry buttons in this phase. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock operational status contract and API shape in tests</name>
  <files>tests/codexbot/test_search_contracts.py, tests/codexbot/test_search_worker.py, tests/codexbot/test_web_api.py</files>
  <read_first>
    tests/codexbot/test_search_contracts.py
    tests/codexbot/test_search_worker.py
    tests/codexbot/test_web_api.py
    src/codexbot/search/contracts.py
    src/codexbot/search/client.py
    src/codexbot/search/queue.py
    src/codexbot/search/state.py
    src/codexbot/web/api.py
  </read_first>
  <action>Add failing tests for typed operational status details. Assert `/api/search/status` and `search_client.get_status()` expose `worker`, `queue`, `progress`, `recent_errors`, `recovery_commands`, and nullable `benchmark` fields. Assert worker details include `status`, `current_task`, `heartbeat_at`, `heartbeat_age_seconds`, `stale`, and `stale_after_seconds`. Assert queue details include `queued_items`, `leased_items`, `failed_items`, `stale_sources`, `oldest_queued_age_seconds`, and `lagging`. Assert recovery command text includes `codexbot-search-worker live-drain-once`, `codexbot-search-worker rebuild`, and `python -m codexbot.search.benchmark`. Assert forbidden substrings such as `Traceback`, `WEB_UI_PASSWORD`, `session.jsonl`, raw `tmp_path`, and `raw transcript content` do not appear in serialized status JSON. Extend existing import-boundary tests so importing `codexbot.web.api` and `codexbot.search.client` does not import `lancedb`, `torch`, `transformers`, or `sentence_transformers`.</action>
  <verify>uv run pytest tests/codexbot/test_search_contracts.py tests/codexbot/test_search_worker.py tests/codexbot/test_web_api.py -q</verify>
  <acceptance_criteria>
    - `tests/codexbot/test_search_contracts.py` asserts operational DTO fields exist.
    - `tests/codexbot/test_search_worker.py` asserts worker heartbeat detail and sanitized recent errors.
    - `tests/codexbot/test_web_api.py` asserts `/api/search/status` returns the new detail fields.
    - Heavy-import regression still covers `web/api.py` and `search/client.py`.
  </acceptance_criteria>
  <done>Backend operational status expectations are locked before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement request-safe operational status DTOs and derivation</name>
  <files>src/codexbot/search/contracts.py, src/codexbot/search/client.py, src/codexbot/search/state.py, src/codexbot/web/api.py, tests/codexbot/test_search_contracts.py, tests/codexbot/test_search_worker.py, tests/codexbot/test_web_api.py</files>
  <read_first>
    tests/codexbot/test_search_contracts.py
    tests/codexbot/test_search_worker.py
    tests/codexbot/test_web_api.py
    src/codexbot/search/contracts.py
    src/codexbot/search/client.py
    src/codexbot/search/queue.py
    src/codexbot/search/state.py
    src/codexbot/web/api.py
  </read_first>
  <action>Add Pydantic DTOs named `SearchWorkerHealth`, `SearchQueueHealth`, `SearchBackfillProgress`, `SearchRecoveryCommand`, `SearchBenchmarkSummary`, and `SearchOperationalStatus` in `contracts.py`. Extend `SearchStatusResponse` with `operations: SearchOperationalStatus | None = None`. Add `DEFAULT_WORKER_STALE_SECONDS = 120` in `search/client.py`, overridable by `CODEXBOT_SEARCH_WORKER_STALE_SECONDS`. Derive `SearchOperationalStatus` inside `get_status()` from `read_worker_status()`, `get_queue_snapshot()`, active generation/manifest/index metadata, and the open session count. Populate `progress.open_sessions`, `progress.indexed_sessions`, `progress.indexed_chunks`, `progress.generation_id`, `progress.model_id`, `progress.vector_dimension`, and `progress.table_name` when available. Populate `recent_errors` only from sanitized worker, queue, or index metadata strings and cap the list to 5. Populate `recovery_commands` with read-only command labels and command strings. Keep `/api/search/status` unchanged structurally except it now serializes the richer typed response from `search_client.get_status()`.</action>
  <verify>uv run pytest tests/codexbot/test_search_contracts.py tests/codexbot/test_search_worker.py tests/codexbot/test_web_api.py -q</verify>
  <acceptance_criteria>
    - Source assertion: `src/codexbot/search/contracts.py` contains `class SearchOperationalStatus`.
    - Source assertion: `SearchStatusResponse` contains `operations: SearchOperationalStatus | None`.
    - Behavior assertion: `get_status(open_session_count=5)` includes `operations.progress.open_sessions == 5`.
    - Behavior assertion: stale or missing queue details do not raise from `get_status()`.
    - Security assertion: serialized status omits raw stack traces, secrets, and local path strings from worker/queue errors.
    - Import assertion: request-path modules still do not load heavy model/index packages.
  </acceptance_criteria>
  <done>Backend status can explain search health and recovery without touching heavy search dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render expandable Web UI status details</name>
  <files>web-ui/src/api.ts, web-ui/src/components/SessionSearch.tsx, web-ui/src/styles.css, tests/codexbot/test_web_ui_search_contract.py</files>
  <read_first>
    web-ui/src/api.ts
    web-ui/src/components/SessionSearch.tsx
    web-ui/src/styles.css
    tests/codexbot/test_web_ui_search_contract.py
    .planning/phases/06-operational-hardening-and-model-tuning/06-UI-SPEC.md
    .planning/phases/06-operational-hardening-and-model-tuning/06-PATTERNS.md
  </read_first>
  <action>Mirror the backend `operations` DTO in `web-ui/src/api.ts`. In `SessionSearch.tsx`, add local `detailsOpen` state and render a real button in `.session-search-meta` with copy `Show details` and `Hide details`, `aria-expanded`, and an `aria-controls` target. Render the expanded region with accessible label `Search status details` below the compact meta row and above filters. Show rows for `Worker heartbeat`, `Queue lag`, `Backfill`, `Recent errors`, `Model/index`, and `Local recovery` when data is present. Use UI-SPEC copy exactly for `Search status`, `Queue lag`, `Worker heartbeat`, `Backfill`, `Recent errors`, `Local recovery`, and `Run the suggested command in the Codi project shell. Web UI recovery controls are intentionally read-only in this phase.` Render recovery command strings as selectable/read-only text, not buttons that call APIs. Keep status polling modest by retaining the existing initial status fetch and add a `window.setInterval` poll of `/api/search/status` no faster than 10 seconds while the component is mounted. Continue refreshing status from search responses.</action>
  <verify>uv run pytest tests/codexbot/test_web_ui_search_contract.py -q && pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/api.ts` exports TypeScript types for the `operations` status payload.
    - `SessionSearch.tsx` contains `Show details`, `Hide details`, `aria-expanded`, and `Search status details`.
    - `SessionSearch.tsx` contains `Worker heartbeat`, `Queue lag`, `Backfill`, `Recent errors`, `Local recovery`, and `Model/index`.
    - `SessionSearch.tsx` contains no Web UI call that mutates search worker/index state.
    - `tests/codexbot/test_web_ui_search_contract.py` asserts the details toggle/copy contract.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Users can inspect operational search details inside the existing sidebar search panel.</done>
</task>

<task type="auto">
  <name>Task 4: Add compact responsive status detail styling</name>
  <files>web-ui/src/styles.css, tests/codexbot/test_web_ui_search_contract.py</files>
  <read_first>
    web-ui/src/styles.css
    tests/codexbot/test_web_ui_search_contract.py
    .planning/phases/06-operational-hardening-and-model-tuning/06-UI-SPEC.md
  </read_first>
  <action>Add CSS for `.search-status-details`, `.search-status-detail-row`, `.search-status-detail-label`, `.search-status-detail-value`, `.search-status-recovery`, `.search-status-command`, and `.search-details-toggle`. Use existing tokens, 6px to 8px radius, 4px/8px/16px spacing, and `overflow-wrap: anywhere` for commands/errors/model ids. Keep details unframed within the sidebar, not a nested card inside a card. Under the existing `@media (max-width: 760px)` section, ensure detail rows wrap and do not add horizontal scrolling or cover composer/terminal controls.</action>
  <verify>uv run pytest tests/codexbot/test_web_ui_search_contract.py -q && pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/styles.css` contains `.search-status-details`.
    - `web-ui/src/styles.css` contains `.search-status-command`.
    - `web-ui/src/styles.css` uses `overflow-wrap: anywhere` for detail values or commands.
    - Mobile media rules mention `.search-status-details` or `.search-status-detail-row`.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Status details remain compact, readable, and mobile-safe.</done>
</task>

</tasks>

<verification>
Run after this plan:

<automated>uv run pytest tests/codexbot/test_search_contracts.py tests/codexbot/test_search_worker.py tests/codexbot/test_web_api.py tests/codexbot/test_web_ui_search_contract.py -q</automated>
<automated>pnpm --dir web-ui build</automated>
</verification>

<success_criteria>
- OPS-03 is covered by backend/API/UI status details for worker heartbeat, queue lag, indexed/open counts, backfill progress, and recent errors.
- OPS-06 is partially covered by clear read-only recovery guidance and a typed place for degraded/benchmark metadata.
- D-01 through D-04 are implemented without adding a new ops panel or browser-side recovery controls.
- Request-path search status remains dependency-light.
</success_criteria>

<output>
After completion, create `.planning/phases/06-operational-hardening-and-model-tuning/06-01-SUMMARY.md`.
</output>
