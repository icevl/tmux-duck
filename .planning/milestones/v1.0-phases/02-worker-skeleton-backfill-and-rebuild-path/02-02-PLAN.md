---
phase: 02-worker-skeleton-backfill-and-rebuild-path
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/codexbot/search/contracts.py
  - src/codexbot/search/state.py
  - src/codexbot/search/backfill.py
  - src/codexbot/search/worker.py
  - src/codexbot/session.py
  - tests/codexbot/test_search_backfill.py
  - tests/codexbot/test_search_worker.py
  - tests/codexbot/test_session.py
  - tests/codexbot/test_transcript_parser.py
autonomous: true
requirements:
  - CORP-01
  - CORP-02
  - INDX-03
must_haves:
  truths:
    - "Backfill corpus is only currently open tmux-backed Codex and Claude sessions."
    - "Backfill uses parser-level `ParsedEntry` values and transcript provenance, not Web UI `HistorySnapshot` payloads."
    - "Every parsed text-bearing entry is eligible for indexing, including user, assistant, thinking, tool use/result, and local command entries."
    - "Long parsed entries produce chunk documents with stable `chunk_index` values."
    - "D-05: Enumerate current window/session state and resolve transcripts through existing runtime/session helpers; do not glob all historical transcripts."
    - "D-06: Consume reusable parsed transcript entries plus transcript source/provenance; avoid coupling to Web UI history DTOs."
    - "D-07: Favor broad recall by indexing every parsed text-bearing entry the parser emits."
    - "D-08: Build chunk documents from parsed entries and preserve `chunk_index` in `SearchRowIdentity`."
    - "D-11: Backfill manifests and document artifacts live under `CODEXBOT_DIR/search`."
    - "D-14: Backfill counters include open sessions, indexed sessions, indexed chunks, failed items, and generation metadata where available."
    - "D-16: Backfill item failures are visible through counters/logs without breaking normal Codi features."
  artifacts:
    - path: "src/codexbot/search/backfill.py"
      provides: "Open-session parser-backed backfill source and chunk document builder"
    - path: "src/codexbot/session.py"
      provides: "Reusable helper for resolving a tmux window to parsed transcript entries with provenance"
    - path: "tests/codexbot/test_search_backfill.py"
      provides: "Codex/Claude open-session source, parser usage, chunking, and failure counter tests"
  key_links:
    - from: "src/codexbot/search/backfill.py"
      to: "src/codexbot/session.py"
      via: "current tmux window to transcript-backed session resolution"
      pattern: "resolve_session_for_window|_read_transcript_entries|TranscriptParser.parse_entries"
    - from: "src/codexbot/search/backfill.py"
      to: "src/codexbot/search/contracts.py"
      via: "TranscriptProvenance and SearchRowIdentity chunk identity"
      pattern: "TranscriptProvenance|SearchRowIdentity"
---

<objective>
Build the real open-session backfill corpus path from current tmux windows through the shared transcript parser into chunk documents with stable search provenance.

Purpose: The worker skeleton must ingest the correct v1 corpus before any generation can be activated or later LanceDB rows can be written.
Output: Parser-backed open-session backfill source, chunk document builder, inactive generation document/manifest write path, and tests for Codex and Claude transcript handling.
</objective>

## Phase Goal

**As a** Codi Web UI user, **I want to** search preparation to cover the Codex and Claude sessions I currently have open, **so that** the future search index matches active work rather than archived transcript noise.

<execution_context>
@/home/vlupanov/.codex/get-shit-done/workflows/execute-plan.md
@/home/vlupanov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-CONTEXT.md
@.planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-RESEARCH.md
@.planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-VALIDATION.md
@.planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-PATTERNS.md
@src/codexbot/search/contracts.py
@src/codexbot/search/state.py
@src/codexbot/search/worker.py
@src/codexbot/session.py
@src/codexbot/tmux_manager.py
@src/codexbot/transcript_parser.py
@tests/codexbot/test_session.py
@tests/codexbot/test_transcript_parser.py

<interfaces>
Existing transcript source:
- `SessionManager.resolve_session_for_window(window_id)` resolves a current tmux window to `CodexSession(session_id, file_path)`.
- `SessionManager._read_transcript_entries()` reads JSONL entries and stamps transcript byte offsets.
- `TranscriptParser.parse_entries()` emits `ParsedEntry` values with role, text, content type, tool id/name, timestamp, transcript offset, and transcript index.

Decision trace:
- Implement D-05 by enumerating open tmux windows and using session resolution, not directory-wide historical scans.
- Implement D-06 by exposing or using a parser-level helper, not `HistorySnapshot`.
- Implement D-07 by including all text-bearing parsed entries.
- Implement D-08 by chunking long entries and preserving chunk indexes in row identity.
- Implement D-11/D-14/D-16 by writing inactive manifest/document artifacts under search state with counters and per-item failure totals.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock open-session backfill source and chunking in tests</name>
  <files>tests/codexbot/test_search_backfill.py, tests/codexbot/test_session.py, tests/codexbot/test_transcript_parser.py, tests/codexbot/test_search_worker.py</files>
  <read_first>
    .planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-CONTEXT.md
    .planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-VALIDATION.md
    .planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-PATTERNS.md
    src/codexbot/session.py
    src/codexbot/tmux_manager.py
    src/codexbot/transcript_parser.py
    src/codexbot/search/contracts.py
    src/codexbot/search/state.py
    src/codexbot/search/backfill.py (create if missing; read if present)
  </read_first>
  <behavior>
    - Test backfill enumerates only `tmux_manager.list_windows()` output and skips closed/unresolved sessions.
    - Test Codex and Claude window states resolve to transcript files through existing session/runtime helpers.
    - Test backfill calls `TranscriptParser.parse_entries()` or a `SessionManager` helper that uses it; tests must not rely on Web UI `HistorySnapshot` messages.
    - Test text-bearing `ParsedEntry` types become documents: `text`, `thinking`, `tool_use`, `tool_result`, and `local_command`.
    - Test image-only or empty-text parser entries do not create text documents unless the parser exposes text.
    - Test a long parsed entry splits into multiple chunks with `chunk_index` 0..N and distinct `SearchRowIdentity` values.
    - Test source provenance includes runtime, session id, transcript source, transcript offset, transcript index, role, content type, tool name/id, timestamp, and current routing metadata separately.
  </behavior>
  <action>Add the tests above using temp transcript files and fake tmux/session state. Keep fixtures local and small. Do not scan real `~/.codex` or `~/.claude` paths.</action>
  <verify>
    <automated>uv run pytest tests/codexbot/test_search_backfill.py tests/codexbot/test_session.py tests/codexbot/test_transcript_parser.py -q</automated>
    Expected RED result before Task 2: failures are limited to missing `codexbot.search.backfill` helpers or the new public parser-backed session helper.
  </verify>
  <acceptance_criteria>
    - Tests prove the v1 corpus starts from open windows, not historical transcript globbing.
    - Tests prove parser-level entries are the source of truth.
    - Tests prove broad text-bearing content coverage and chunk-index identity.
  </acceptance_criteria>
  <done>Open-session backfill source and chunking behavior are specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parser-backed open-session backfill and inactive document artifacts</name>
  <files>src/codexbot/search/contracts.py, src/codexbot/search/state.py, src/codexbot/search/backfill.py, src/codexbot/search/worker.py, src/codexbot/session.py, tests/codexbot/test_search_backfill.py, tests/codexbot/test_search_worker.py</files>
  <read_first>
    tests/codexbot/test_search_backfill.py
    tests/codexbot/test_search_worker.py
    src/codexbot/session.py
    src/codexbot/transcript_parser.py
    src/codexbot/tmux_manager.py
    src/codexbot/search/contracts.py
    src/codexbot/search/state.py
    src/codexbot/search/worker.py
  </read_first>
  <action>Implement `src/codexbot/search/backfill.py` with an open-session source that enumerates current tmux windows, resolves transcript-backed Codex/Claude sessions, reads parsed transcript entries through a reusable `SessionManager` helper, and yields chunk documents carrying `TranscriptProvenance`, `SearchRowIdentity`, routing metadata, text, timestamp/order fields, and chunk indexes. Add bounded deterministic chunking for long parsed entry text. Integrate the worker enough to materialize an inactive generation directory with `documents.jsonl` and `manifest.json` counters, but do not mark the generation active in this plan. Keep failures item-scoped where possible and increment failed counters without aborting unrelated sessions. Do not add LanceDB, embeddings, live queue, retrieval, or UI work.</action>
  <verify>
    <automated>uv run pytest tests/codexbot/test_search_backfill.py tests/codexbot/test_search_worker.py tests/codexbot/test_session.py tests/codexbot/test_transcript_parser.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: no code path in backfill glob-scans all historical `~/.codex/sessions` or `~/.claude/projects` as the corpus.
    - Source assertion: backfill uses `TranscriptParser.parse_entries()` through session/backfill helpers, not Web UI history DTOs.
    - Behavior assertion: Codex and Claude current-session transcripts both produce chunk documents.
    - Behavior assertion: long text entries produce multiple chunk documents with stable `chunk_index`.
    - Behavior assertion: inactive generation document and manifest artifacts are written only under `CODEXBOT_DIR/search`.
    - CLI assertion: targeted tests pass.
  </acceptance_criteria>
  <done>Worker backfill can produce an inactive, parser-backed open-session document corpus with manifest counters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tmux/session state to search corpus | Current windows and mutable routing metadata select the v1 corpus but must not define stable row identity. |
| transcript files to search documents | Local JSONL transcript content is parsed and chunked into derived documents without leaking into status errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure / Scope Creep | backfill source | mitigate | Enumerate current tmux windows only; tests reject historical directory scans. |
| T-02-03 | Tampering | inactive generation artifacts | mitigate | Write documents and manifests under `CODEXBOT_DIR/search`; do not mutate authoritative session/monitor state. |
| T-02-04 | Tampering | row identity and chunks | mitigate | Use transcript provenance plus chunk index for identity; keep window metadata as routing/display only. |
| T-02-06 | Information Disclosure | backfill errors and counters | mitigate | Count failed items and log sanitized summaries; do not expose raw transcript bodies or local stacks through status. |
</threat_model>

<verification>
Run after this plan:

<automated>uv run pytest tests/codexbot/test_search_backfill.py tests/codexbot/test_search_worker.py tests/codexbot/test_session.py tests/codexbot/test_transcript_parser.py -q</automated>
</verification>

<success_criteria>
- CORP-01 is covered by normalized Codex and Claude transcript parsing.
- CORP-02 is covered by user/assistant/tool/thinking/local-command text-bearing document creation.
- INDX-03 is covered by current open-session enumeration and backfill.
- D-05, D-06, D-07, D-08, D-11, D-14, and D-16 are represented in code and tests.
</success_criteria>

<output>
Create `.planning/phases/02-worker-skeleton-backfill-and-rebuild-path/02-02-SUMMARY.md` when done.
</output>
