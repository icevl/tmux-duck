---
phase: 05-web-ui-search-experience-and-navigation
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/codexbot/web/api.py
  - tests/codexbot/test_web_api.py
  - web-ui/src/api.ts
  - web-ui/src/App.tsx
  - web-ui/src/components/ChatView.tsx
  - web-ui/src/components/SessionSearch.tsx
  - web-ui/src/styles.css
autonomous: true
requirements: [WEB-03, WEB-04, WEB-05, WEB-06, WEB-07]
user_setup: []
must_haves:
  truths:
    - "D-05 WEB-03: Search result group selection opens the owning live session by current tmux window_id."
    - "D-06 WEB-04 WEB-06: Nested hit selection loads a bounded message window around transcript_offset/transcript_index without loading the full transcript."
    - "D-07 WEB-04: The matched transcript message receives a temporary visible Search hit label/highlight after navigation."
    - "D-08 WEB-05: If exact hit coordinates cannot be loaded, the owning session remains open and the UI shows Opened session. Exact hit is unavailable."
    - "Active user input prompts remain active at the bottom after search navigation until the user answers them."
  artifacts:
    - path: "src/codexbot/web/api.py"
      provides: "around_offset and around_index support for bounded message history windows"
      contains: "around_offset"
    - path: "tests/codexbot/test_web_api.py"
      provides: "Regression tests for around-message query behavior"
      contains: "around_offset"
    - path: "web-ui/src/App.tsx"
      provides: "Search hit target routing from Sidebar to ChatView"
      contains: "searchTarget"
    - path: "web-ui/src/components/ChatView.tsx"
      provides: "Coordinate-based target loading, scrolling, and temporary highlight"
      contains: "Search hit"
  key_links:
    - from: "web-ui/src/components/SessionSearch.tsx"
      to: "web-ui/src/App.tsx"
      via: "onOpenHit callback with routing.window_id and transcript coordinates"
      pattern: "onOpenHit"
    - from: "web-ui/src/App.tsx"
      to: "web-ui/src/components/ChatView.tsx"
      via: "searchTarget prop"
      pattern: "searchTarget"
    - from: "web-ui/src/components/ChatView.tsx"
      to: "/api/sessions/{window_id}/messages"
      via: "api.getMessages with around_offset and around_index"
      pattern: "around_offset"
---

<objective>
Implement exact search-hit navigation from grouped results into the chat transcript.

Purpose: Users need result hits to open the owning tmux-backed session, load only a bounded message window, focus the matching transcript message, and fall back safely when exact coordinates are unavailable.
Output: Backend around-window support with tests, frontend search target routing, ChatView bounded target loading, scroll/highlight behavior, and fallback toast handling.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-CONTEXT.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-RESEARCH.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-UI-SPEC.md
@.planning/phases/05-web-ui-search-experience-and-navigation/05-PATTERNS.md
@src/codexbot/web/api.py
@tests/codexbot/test_web_api.py
@web-ui/src/api.ts
@web-ui/src/App.tsx
@web-ui/src/components/ChatView.tsx
@web-ui/src/components/SessionSearch.tsx
@web-ui/src/styles.css
</context>

<threat_model>
ASVS level: 1
Block on: high

| Threat | Severity | Component | Mitigation |
| --- | --- | --- | --- |
| T-05-04 Search result routes to a stale or wrong session by name/session_id instead of tmux window_id. | high | `SessionSearch.tsx`, `App.tsx` | Use only `routing.window_id` for opening sessions; names and runtime session IDs stay display/provenance metadata. |
| T-05-05 Hit navigation loads an unbounded transcript or skips the target message. | high | `api.py`, `ChatView.tsx` | Add `around_offset`/`around_index` backend slicing that includes the target when present and respects the existing `limit <= 2000` bound. |
| T-05-06 Search navigation deactivates or reorders active user-input choices. | high | `ChatView.tsx` | Preserve the existing `activeChoiceMessage` bottom-rendering path; target scrolling/highlight must not move the active prompt into historical order. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded around-message query support</name>
  <files>src/codexbot/web/api.py, tests/codexbot/test_web_api.py</files>
  <read_first>src/codexbot/web/api.py, tests/codexbot/test_web_api.py, .planning/phases/05-web-ui-search-experience-and-navigation/05-RESEARCH.md</read_first>
  <action>Extend `get_messages` query params with `around_offset: int | None = Query(None, ge=0)` and `around_index: int | None = Query(None, ge=0)`. When `around_offset` is provided, compute target order `(around_offset, around_index or 0)`, find the first message whose `_order_value(message)` is greater than or equal to the target, and return a bounded slice centered around that index. If every ordered message is lower than the target, return the final bounded slice. Preserve existing `before_offset`, `after_offset`, `before`, and `after` behavior when `around_offset` is not provided. Keep the existing `limit` guard. Add tests proving `around_offset=10&around_index=1&limit=2` returns the target message and one neighbor from a three-message fixture, and that `after_offset` behavior still returns `second`, `third` in the existing test.</action>
  <verify>uv run pytest tests/codexbot/test_web_api.py -q</verify>
  <acceptance_criteria>
    - `src/codexbot/web/api.py` contains `around_offset`.
    - `src/codexbot/web/api.py` contains `around_index`.
    - `tests/codexbot/test_web_api.py` contains a test name including `around`.
    - `uv run pytest tests/codexbot/test_web_api.py -q` exits 0.
  </acceptance_criteria>
  <done>Message API can return a bounded window containing a search target coordinate.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend search target types and API params</name>
  <files>web-ui/src/api.ts, web-ui/src/components/SessionSearch.tsx</files>
  <read_first>web-ui/src/api.ts, web-ui/src/components/SessionSearch.tsx, src/codexbot/search/contracts.py</read_first>
  <action>Add `around_offset?: number` and `around_index?: number` to `api.getMessages` options and URL parameter serialization. Export a `SearchHitTarget` interface or equivalent from a frontend module that includes `target_id`, `window_id`, optional `session_id`, optional `transcript_offset`, optional `transcript_index`, optional `chunk_index`, `source_order`, and fallback display text. Update nested hit buttons in `SessionSearch` so `onOpenHit(target)` receives `routing.window_id`, `hit.provenance.session_id`, `hit.provenance.transcript_offset`, `hit.provenance.transcript_index`, `hit.identity.chunk_index`, `hit.source_order`, and a unique target nonce. Do not call `api.getMessages` from `SessionSearch`.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/api.ts` serializes `around_offset`.
    - `web-ui/src/api.ts` serializes `around_index`.
    - `web-ui/src/components/SessionSearch.tsx` contains `onOpenHit`.
    - `web-ui/src/components/SessionSearch.tsx` passes `routing.window_id` into hit target construction.
    - `web-ui/src/components/SessionSearch.tsx` does not call `api.getMessages`.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Hit rows produce a concrete navigation target without fetching transcript history in the search component.</done>
</task>

<task type="auto">
  <name>Task 3: Route search targets through App</name>
  <files>web-ui/src/App.tsx, web-ui/src/components/Sidebar.tsx</files>
  <read_first>web-ui/src/App.tsx, web-ui/src/components/Sidebar.tsx, web-ui/src/components/SessionSearch.tsx</read_first>
  <action>Add App-level `searchTarget` state. Add a `handleOpenSearchHit(target)` callback that sets `activeId` to `target.window_id`, stores the target, closes the sidebar on mobile by setting `sidebarOpen` false, and leaves the existing session draft cache untouched. Wire this callback through `Sidebar` into `SessionSearch`. Pass `searchTarget` and `onSearchTargetFallback` or the existing `showToast` path into `ChatView`. Use the exact fallback text `Opened session. Exact hit is unavailable.` when `ChatView` reports that target coordinates could not be loaded. Group/header selection continues to use the existing `handleSelectSession` path.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/App.tsx` contains `searchTarget`.
    - `web-ui/src/App.tsx` contains `Opened session. Exact hit is unavailable.`
    - `web-ui/src/App.tsx` passes `searchTarget=` to `ChatView`.
    - `web-ui/src/components/Sidebar.tsx` passes an `onOpenHit` callback to `SessionSearch`.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Search hit navigation selects the owning session and provides ChatView with the exact target.</done>
</task>

<task type="auto">
  <name>Task 4: Load, scroll, and highlight target messages in ChatView</name>
  <files>web-ui/src/components/ChatView.tsx, web-ui/src/styles.css</files>
  <read_first>web-ui/src/components/ChatView.tsx, web-ui/src/api.ts, web-ui/src/styles.css, .planning/phases/05-web-ui-search-experience-and-navigation/05-UI-SPEC.md</read_first>
  <action>Add `searchTarget` and fallback callback props to `ChatView`. Add a coordinate key helper using `transcript_offset` and `transcript_index` from `messageTranscriptPosition`. When a new target nonce arrives for the current `session.window_id`, first search loaded messages for a matching coordinate. If absent and `transcript_offset` is present, call `api.getMessages(session.window_id, { around_offset: target.transcript_offset, around_index: target.transcript_index ?? 0, limit: 120 })`, merge the returned messages with `mergeAppendMessages`, and retry the coordinate lookup. If found, set a temporary highlighted key, scroll the row with `data-search-target-key` or `data-msg-key` into view, and clear the highlight after 3 to 5 seconds. Add visible label text `Search hit` for the highlighted row. If no coordinates are present or the around fetch cannot locate the row, invoke the fallback callback while leaving the owning session open. Preserve `activeChoiceMessage` filtering and bottom rendering exactly: search highlight may apply to historical rows but must not remove, disable, or move the active choice panel.</action>
  <verify>pnpm --dir web-ui build</verify>
  <acceptance_criteria>
    - `web-ui/src/components/ChatView.tsx` contains `Search hit`.
    - `web-ui/src/components/ChatView.tsx` calls `api.getMessages` with `around_offset`.
    - `web-ui/src/components/ChatView.tsx` still contains `activeChoiceMessage && (` bottom-rendering branch.
    - `web-ui/src/styles.css` contains `.messages-row.search-hit`.
    - `pnpm --dir web-ui build` exits 0.
  </acceptance_criteria>
  <done>Nested search hit selection loads a bounded target window, scrolls to the matching message, highlights it, and preserves input-required prompts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/codexbot/test_web_api.py -q` exits 0.
- [ ] `uv run pyright src/codexbot/` exits 0.
- [ ] `pnpm --dir web-ui build` exits 0.
- [ ] `rg -n "around_offset" src/codexbot/web/api.py tests/codexbot/test_web_api.py web-ui/src/api.ts web-ui/src/components/ChatView.tsx` shows all four surfaces.
- [ ] Active choice prompt rendering remains as the final rendered message row when present.
</verification>

<success_criteria>

- Search group rows open live sessions by tmux `window_id`.
- Nested hit rows can load a bounded message window around target transcript coordinates.
- Matching messages receive a temporary visible `Search hit` highlight.
- Hit navigation failure still opens the owning session and reports the exact fallback copy.
- Search navigation does not deactivate or reorder active user-input prompts.

</success_criteria>

<output>
After completion, create `.planning/phases/05-web-ui-search-experience-and-navigation/05-02-SUMMARY.md`.
</output>
